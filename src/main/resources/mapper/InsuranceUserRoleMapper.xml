<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.InsuranceUserRoleMapper">

    <select id="getByUserId" parameterType="Integer" resultType="com.po.InsuranceUserRole">
    select * from insae_user_role where userid=#{userId}
    </select>

</mapper>